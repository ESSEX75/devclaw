/** Exposes managed projection reconciliation to application use cases. */
export type { ManagedProjectionResult } from "./reconcile-managed-labels.js";
export { applyManagedLabelDiff, reconcileManagedLabels, reconcileManagedLabelsLocked } from "./reconcile-managed-labels.js";
